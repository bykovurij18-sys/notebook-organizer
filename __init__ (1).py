# Notebook Organizer

**Автор:** Manus AI (по заданию пользователя)

## Описание программы

**Notebook Organizer** — это мощное консольное приложение для управления заметками, написанное на Python с использованием принципов объектно-ориентированного программирования (ООП). 

Приложение поддерживает различные типы заметок (полиморфизм), сохранение данных в формате JSON, фильтрацию, а также уникальную функцию **стека отмены действий (UndoStack)**, позволяющую отменять случайные удаления или изменения.

### Основные возможности:
- **Полиморфизм заметок:**
  - `TextNote` — обычные текстовые заметки.
  - `VoiceNote` — голосовые заметки (хранят длительность и транскрипцию).
  - `ChecklistNote` — чеклисты с возможностью отмечать выполненные пункты.
- **CRUD-операции:** Создание, чтение, обновление и удаление заметок.
- **Стек отмены (Undo):** Возможность отменить последнее действие (добавление, удаление, редактирование).
- **Фильтрация и поиск:** Поиск по тегам, дате, типу заметки и тексту заголовка.
- **Хранение данных:** Автоматическое сохранение и загрузка из JSON-файла с созданием резервных копий (`.bak`).
- **Интерактивный CLI:** Удобное консольное меню с поддержкой цвета.

---

## Как запустить приложение

### Требования
- Python 3.9 или выше.
- Стандартная библиотека Python (дополнительные зависимости не требуются для работы самого приложения).

### Запуск
1. Склонируйте репозиторий или скачайте исходный код.
2. Перейдите в корневую директорию проекта:
   ```bash
   cd notebook_organizer
   ```
3. Запустите главный файл:
   ```bash
   python3 main.py
   ```

*Примечание: По умолчанию заметки сохраняются в файл `~/.notebook_organizer/notes.json`. Вы можете указать свой путь с помощью флага `--storage`:*
```bash
python3 main.py --storage /путь/к/вашему/файлу.json
```

### Запуск тестов
Для запуска тестов потребуется пакет `pytest`:
```bash
pip install pytest
pytest tests/ -v
```

---

## Примеры использования

После запуска `python3 main.py` вы увидите главное меню:

```text
  NOTEBOOK ORGANIZER  (0 заметок)
  ────────────────────────────────────────────────────────────
  1.  Список заметок
  2.  Создать заметку
  3.  Просмотреть заметку
  4.  Редактировать заметку
  5.  Удалить заметку
  ────────────────────────────────────────────────────────────
  6.  Фильтр по тегу
  7.  Фильтр по дате
  8.  Фильтр по типу
  9.  Поиск по заголовку
  ────────────────────────────────────────────────────────────
  10. Отменить последнее действие
  11. История действий
  ────────────────────────────────────────────────────────────
  12. Сохранить заметки
  13. Загрузить заметки
  ────────────────────────────────────────────────────────────
  0.  Выход
```

### Сценарий 1: Создание и просмотр чеклиста
1. Выберите `2` (Создать заметку).
2. Выберите тип `3` (Чеклист).
3. Введите заголовок: `Покупки`.
4. Введите теги: `дом, магазин`.
5. Введите пункты (по одному на строку, пустая строка для завершения): `Молоко`, `Хлеб`, `Яблоки`.
6. Выберите `1` (Список заметок), чтобы увидеть созданный чеклист.

### Сценарий 2: Использование стека отмены
1. Выберите `5` (Удалить заметку) и удалите любую существующую заметку.
2. Поймите, что удалили не ту заметку!
3. Выберите `10` (Отменить последнее действие).
4. Приложение сообщит: `✓ Отменено: Удаление заметки: «Название»`. Заметка снова в списке!

### Сценарий 3: Фильтрация
1. Выберите `6` (Фильтр по тегу).
2. Введите тег, например, `работа`.
3. Приложение выведет только те заметки, которые содержат этот тег.
